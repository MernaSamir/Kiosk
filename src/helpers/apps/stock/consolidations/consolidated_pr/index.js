import summary from './summary'
import add_new from './new'

const consolidated_pr = {
    name: "Consolidated PR (CPR)",
    last: true,
    sub:[
        summary,
        add_new
    ]
}

export default consolidated_pr;