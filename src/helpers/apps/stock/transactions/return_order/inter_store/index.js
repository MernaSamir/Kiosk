import summary from './summary'
import add_new from './add_new'

const inter_store = {
    name: "Inter Store (ReO)",
    last: true,
    sub:[
        summary,
        add_new
    ]
}

export default inter_store;