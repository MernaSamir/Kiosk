import inter_store from './inter_store'
// import vendor from './vendor'

const receiving_order = {
    name: "Receiving Order (RO)",
    sub:[
        inter_store,
        // vendor
    ]
}

export default receiving_order;