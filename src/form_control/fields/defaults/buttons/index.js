import {lazy} from 'react'
export const button = lazy(()=>import('./main'));
export const button_path = lazy(()=>import('./path'));
export const popover = lazy(()=>import('./popover'));
export const extra_buttons = lazy(()=>import('./popover'));